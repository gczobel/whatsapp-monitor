services:
  whatsapp-monitor:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./config:/app/config
      - ./data:/app/data
      - ./sessions:/app/sessions
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      - PORT=3000
      - LOG_LEVEL=info
    restart: unless-stopped
